import 'package:flutter/material.dart';

class LiveStreamList extends StatelessWidget {
  const LiveStreamList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
